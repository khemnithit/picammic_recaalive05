paths:
  screen:
    runOnInit: >
      ffmpeg -f x11grab -s 1280x720 -i :0.0+0,150 
      -f alsa -i plughw:CARD=Blink500B2,DEV=0
      -c:v libx264 -preset faster -b:v 6000k 
      -f flv rtmp://a.rtmp.youtube.com/live2/
      -f rtsp rtsp://127.0.0.1:8554/screen
    runOnInitRestart: yes

    record: true
    recordPath: ./recordings/%path/%Y-%m-%d_%H-%M-%S-%f
    recordFormat: fmp4

    recordPartDuration: 1s
    recordMaxPartSize: 50M
    recordSegmentDuration: 1h
    recordDeleteAfter: 8d
    
